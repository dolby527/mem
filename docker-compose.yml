services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: mem
      POSTGRES_PASSWORD: mem
      POSTGRES_DB: mem
    ports:
      - "5432:5432"
    volumes:
      - mem_pg_data:/var/lib/postgresql/data

volumes:
  mem_pg_data:
