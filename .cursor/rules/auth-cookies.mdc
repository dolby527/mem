---
description: 인증, 쿠키, middleware, API 클라이언트 패턴 (추가 예정)
globs: apps/web/src/middleware.ts,apps/web/src/lib/auth/**,apps/web/src/lib/api/**,apps/web/src/providers/**,apps/api/src/auth/**
alwaysApply: false
---

# Auth & cookies (planned)

- Snack 패턴 참고: httpOnly `accessToken` / `refreshToken`
- JWT에 `hospitalId` 포함; API guard에서 리소스와 일치 검증
- 클라이언트: `cookieFetch`, `credentials: 'include'`
